# ifndef MINITALK_H

#include <stdio.h>
#include <signal.h>
#include <stdlib.h>
#include <unistd.h>


char	*ft_strdup(const char *str);
size_t     ft_strlen(const char *str);
char	*ft_itoa(int n);

# endif